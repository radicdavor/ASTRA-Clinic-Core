# Program 1 Phase G4 Regression Notes

The patient-scoped timeline read API prototype was added as GET-only.

Runtime surface:

- `GET /api/patients/{patient_id}/clinical-evidence-timeline`

No write/review routes were added.

