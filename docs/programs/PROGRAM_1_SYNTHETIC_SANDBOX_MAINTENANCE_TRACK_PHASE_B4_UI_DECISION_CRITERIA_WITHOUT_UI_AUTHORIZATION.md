# Program 1 Synthetic Sandbox Maintenance Track Phase B4 - UI Decision Criteria Without UI Authorization

Status: documentation-only decision criteria. Synthetic-only. Non-production. No-UI Hold preserving. Not for clinical use.

Phase B may ask UI-readiness questions, but it does not authorize UI work.

Questions before any future UI review:

- Is the terminal workflow understandable enough to justify UI discussion?
- Which exact terminal sections would need visual organization?
- Would UI improve comprehension, or would documentation cleanup be enough?
- Which safety banners must remain visible in any future concept?
- Which actions must remain absent from any future UI concept?
- Would UI risk implying production, clinical use, patient-facing use, or workflow action?
- What review gate would be required before any local UI prototype could be considered?

Current decision:

- No web UI is authorized.
- No browser UI is authorized.
- No local web app is authorized.
- No server/API is authorized.
- No hosted preview is authorized.
- No UI track is started.
