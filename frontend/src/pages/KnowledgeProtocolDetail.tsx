import { useParams } from "react-router-dom";
import { api } from "../api/client";
import { ActionButton } from "../components/ActionButton";
import { useApi } from "../hooks/useApi";
import { KnowledgeProtocol } from "../types";
export function KnowledgeProtocolDetail() { const {id}=useParams(); const protocol=useApi<KnowledgeProtocol|null>(`/api/knowledge-protocols/${id}`,null); if(!protocol.data)return <div className="page">Učitavanje...</div>; const refresh=async(path:string)=>protocol.setData(await api<KnowledgeProtocol>(path,{method:"POST"})); return <div className="page knowledge-detail"><header className="page-header"><div><span className="eyebrow">{protocol.data.specialty} · verzija {protocol.data.version}</span><h1>{protocol.data.title}</h1><p>{protocol.data.summary}</p></div><span className={`knowledge-state ${protocol.data.status}`}>{protocol.data.status}</span></header><section className="panel provenance-line"><div><span>Tvrdnja</span><strong>{protocol.data.rules.length} strukturiranih pravila</strong></div><div><span>Pregled</span><strong>{protocol.data.status === "reviewed" ? "Liječnički pregledano" : "Nije službeno"}</strong></div><div><span>Izvor</span><a href={protocol.data.source_url} target="_blank" rel="noreferrer">{protocol.data.source_title}</a></div></section><section className="knowledge-rules">{protocol.data.rules.map(rule=><article className="panel" key={rule.id}><span>{rule.evidence_level||"Razina dokaza nije navedena"}</span><h2>{rule.label}</h2><h3>Kada razmotriti</h3><p>{rule.condition_text}</p><h3>Referentna smjernica</h3><p>{rule.guidance_text}</p><small>Ovo nije automatska odluka niti preporuka za konkretnog pacijenta.</small></article>)}</section><div className="quick-actions">{protocol.data.status==="draft"&&<ActionButton variant="update" onClick={()=>refresh(`/api/knowledge-protocols/${id}/review`)} helpTitle="Potvrdi pregled" help="Liječnik potvrđuje izvor i sadržaj protokola.">Potvrdi liječnički pregled</ActionButton>}{protocol.data.status!=="archived"&&<ActionButton variant="danger" confirmMessage="Arhivirati ovaj protokol?" onClick={()=>refresh(`/api/knowledge-protocols/${id}/archive`)} helpTitle="Arhiviraj" help="Uklanja protokol iz aktivne službene knjižnice.">Arhiviraj</ActionButton>}</div></div>; }
